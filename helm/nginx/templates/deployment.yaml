apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-argo.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hello-argo.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "hello-argo.name" . }}
    spec:
      containers:
      - name: nginx
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html
        configMap:
          name: {{ include "hello-argo.fullname" . }}